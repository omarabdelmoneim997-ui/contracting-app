# Omar ERP — Current Project Context

آخر تحديث: 2026-08-27 (تحديث ثانٍ — إعادة تصميم تبويب "التكاليف" كـ Pivot Table)

## المشروع

Omar ERP هو نظام إدارة لشركة مقاولات، باللغة العربية RTL، يركز على إدارة المشروعات وتكاليفها وإيراداتها وتقاريرها.

## التقنية

* React 18 + Vite
* Tailwind CSS
* Recharts
* lucide-react
* Supabase (Postgres + supabase-js)
* Vercel + GitHub
* المستخدم يرفع الملفات يدويًا من GitHub Web، ولا يستخدم Git CLI.

## الملفات الأساسية

```text
contracting-app/
├── index.html
├── vite.config.js
├── package.json
└── src/
    ├── main.jsx
    ├── App.jsx
    └── supabaseClient.js
```

`App.jsx` يحتوي معظم الواجهة والمكونات.

## قاعدة البيانات

Supabase project ref:
`pywkkzedqqgmaghdqxba`

الجداول:

* projects (يشمل عمود `treasury_opening_balance` لرصيد بداية الخزينة اليدوي)
* work_items
* costs (يشمل أعمدة `cost_level_1` و`cost_level_2` لتصنيف فرعي اختياري للتكلفة)
* extracts
* collections
* app_users
* treasury_entries (حركات خزينة كل مشروع: إيداع/صرف)
* finance_persons (أشخاص/جهات السلف والتمويلات — **مستقلة عن المشروعات**)
* finance_transactions (حركات كل شخص: تمويل / سلفة / سداد، في جدول مسطّح واحد)

العلاقات الأساسية:

* project → work_items: Cascade
* project → costs: Cascade
* project → extracts: Cascade
* extracts → collections: Cascade
* work_items → costs: **يتم حذف التكاليف المرتبطة صراحةً من الكود قبل حذف بند العمل** (حذف نهائي وليس SET NULL — الكود ينفّذ `delete from costs where work_item_id = id` ثم يحذف البند)
* project → treasury_entries: Cascade
* finance_persons → finance_transactions: **مستقل تمامًا عن جدول projects** — لا توجد علاقة بمشروع. عند حذف شخص/جهة، يتم حذف حركاته من الكود صراحةً (`delete from finance_transactions where person_id = id`) ثم حذف الشخص.

عند حذف Work Item يتم حذف التكاليف المرتبطة به صراحةً من الكود قبل حذف البند.
عند حذف مشروع، يتم حذف بنود الأعمال والتكاليف والمستخلصات والتحصيلات وحركات الخزينة الخاصة به من الـ state صراحةً بعد نجاح حذف المشروع من قاعدة البيانات.

كل الجداول عليها RLS مفعّل مع policy عامة `using (true) with check (true)`، بنفس نمط باقي الجداول العادية (فيما عدا `app_users`، انظر قسم Login).

## Login

يوجد جدول `app_users` يحتوي username/password.
RLS مفعّل بدون policies.
التحقق يتم فقط عن طريق:
`verify_login(username, password)`
وهي Security Definer ومسموحة للـ anon.

لا يتم حفظ تسجيل الدخول في localStorage أو sessionStorage. المستخدم يجب أن يسجل الدخول مرة أخرى بعد إغلاق المتصفح.

**مهم:** لا تعرض كلمة المرور في الشات.

## الحالة الحالية

المشروع منشور ويعمل على Vercel Production، وكل المميزات الحالية تعمل.

### المميزات الموجودة

* إنشاء / تعديل / حذف المشروعات، وحذف المشروع بالكامل مع البيانات المرتبطة
* إدارة بنود الأعمال: إضافة / تعديل (الاسم فقط من الواجهة حاليًا) / حذف
* إدارة التكاليف (أنواعها: مشتريات، مصنعيات، مصروفات، عهد، مصروفات عمومية بأصناف جاهزة)، مع حقلين اختياريين "المستوى الأول/الثاني" للتصنيف
* **تبويب "التكاليف" — عرض Pivot Table (جديد):**

  * التكاليف تُعرض مُجمّعة على مستويين بدلاً من جدول مسطّح:
    1. **المستوى الأول: بند العمل** — كل بند عمل (مثل "أعمال المباني") يظهر كصف رأسي قابل للطي/الفتح، وبجانبه إجمالي كل التكاليف المرتبطة به. التكاليف غير المرتبطة بأي بند تظهر في مجموعة منفصلة "تكاليف غير مرتبطة ببند" في آخر القائمة.
    2. **المستوى الثاني: تفاصيل بند التكلفة (الوصف)** — داخل كل بند عمل، تتجمّع التكاليف حسب نص الوصف المُدخل عند التسجيل (مثال: تحت "أعمال المباني" تظهر مجموعتان "مصنعيات المباني" و"توريدات الطوب" لو كانت هذه هي الأوصاف المستخدمة)، مع عرض: نوع/أنواع التكلفة (badges ملوّنة)، عدد الحركات، إجمالي الكمية والوحدة (لو كانت كل الحركات بنفس الوحدة)، وإجمالي القيمة.
    3. **فتح البند الفرعي** يعرض كل الحركات (السجلات) الفردية المكوّنة له: التاريخ، الكمية × السعر، المستوى الأول/الثاني للتصنيف (لو موجود)، القيمة، وأزرار تعديل/حذف لكل حركة (نفس آلية التعديل السابقة عبر النموذج العلوي).
  * صف إجمالي عام أسفل الجدول.
  * الفلترة حسب نوع التكلفة (زي ما كانت) لسه شغالة وتؤثر على محتوى الـ Pivot بالكامل (تعيد بناء المجموعات بناءً على النتائج المفلترة).
  * الإضافة والتعديل والحذف الفردي للتكلفة لسه بنفس الشكل والحقول القديمة (نموذج علوي واحد يظهر/يختفي)، والفرق الوحيد هو طريقة *عرض* التكاليف تحت بعضها.
* المستخلصات: إضافة / تعديل / حذف، والتحصيلات: إضافة / حذف، وطباعة المستخلص PDF عبر `window.print()`
* خزينة المشروع: رصيد افتتاحي يدوي، حركات إيداع/صرف، تجميع يومي تلقائي (رصيد أول/آخر اليوم)، تعديل/حذف
* حسابات السلف والتمويلات (قسم عام مستقل، غير مرتبط بأي مشروع): أشخاص/جهات، حركات (تمويل/سلفة/سداد)، كشف حساب تراكمي لكل شخص
* Dashboard: إحصائيات + رسم بياني ميزانية/فعلي لكل بند + آخر التكاليف
* Budget vs Actual (تبويب "المقايسة")
* RTL / Arabic UI بالكامل، خط Cairo وIBM Plex Mono للأرقام

## الطباعة

طباعة المستخلص موجودة وتعمل في Production عبر نافذة HTML مخصصة و`window.print()`. لا توجد مكتبة PDF إضافية.

## Build / Deploy

آخر تعديل (Pivot Table للتكاليف) تم فحصه محليًا للتأكد من سلامة بناء الجملة (JSX syntax) قبل التسليم، لكن لم يُشغَّل بعد Build كامل على Vercel — يُنصح بعمل build محلي أو مراقبة أول Deploy بعد رفع الملف.

Vercel متصل بـ GitHub `main` ويعمل Auto Deploy بعد كل Commit.

## Known Issues

1. Bundle كبير نسبيًا وفي ازدياد تدريجي.
2. Passwords في `app_users` مخزنة Plain Text — أداة داخلية حاليًا.
3. لا توجد Multi-user permissions حاليًا.
4. حذف بند العمل يحذف التكاليف المرتبطة به حذفًا نهائيًا (وليس SET NULL) — لا يمكن التراجع، ويُطلب تأكيد المستخدم قبل التنفيذ.
5. وحدة "السلف والتمويلات" غير مرتبطة بمشروع محدد إطلاقًا (سلوك مقصود حاليًا).
6. لا يوجد نموذج ظاهر في الواجهة لتعديل الكمية/سعر الوحدة لبند العمل مباشرة.
7. لا يوجد ربط تلقائي بين تحصيل مستخلص وحركة إيداع في الخزينة.
8. استخدام `alert()` و`window.confirm()` بدلاً من مكوّنات UI متسقة.
9. توليد المعرفات (IDs) على العميل عبر `Math.random().toString(36)`.
10. **(جديد)** التجميع الفرعي في تبويب التكاليف يعتمد على **تطابق نص الوصف حرفيًا** (بعد `trim()` فقط) — لو المستخدم كتب نفس البند بصياغات مختلفة (مثلاً "توريد طوب" مرة و"توريد الطوب" مرة تانية) هيتحسبوا كبندين فرعيين منفصلين بدل بند واحد مجمّع. لا يوجد حاليًا قائمة موحّدة/autocomplete لأوصاف التكلفة تمنع هذا التشتت.
11. **(جديد)** لا يوجد فرز يدوي للبنود الفرعية داخل كل بند عمل — الترتيب حاليًا تلقائي حسب إجمالي القيمة تنازليًا.

## Backlog

المهام القادمة:

1. Multi-users & Permissions
2. Inventory Module
3. Reminders / Scheduling
4. Attachments / Documents
5. نموذج تعديل كمية/سعر بند العمل من الواجهة مباشرة
6. تقييم الحاجة لربط "حسابات السلف والتمويلات" بمشروع محدد (اختياري)
7. **(جديد، مقترح)** إضافة قائمة موحّدة (autocomplete) لأوصاف التكلفة الشائعة لكل بند عمل، لتقليل تشتت التجميع في الـ Pivot Table الجديد بسبب اختلاف الصياغة
8. **(جديد، مقترح)** خيار "طي/فتح الكل" لمجموعات التكاليف دفعة واحدة، لو عدد بنود الأعمال أو التفاصيل زاد بشكل كبير

## أهم قواعد العمل

* كل البيانات يجب أن تأتي من Supabase.
* ممنوع الرجوع إلى Seed/Mock Local Data.
* `app_users` يجب أن يظل مقفولًا بدون Read Policy.
* الوصول لتسجيل الدخول فقط عبر `verify_login`.
* لا تضف localStorage/sessionStorage لحفظ الجلسة.
* المستخدم يرفع الكود يدويًا عبر GitHub Web.

### عند تعديل App.jsx

لا تعطِ المستخدم Patch جزئي. أعطه **الملف كاملًا**.

تعليمات GitHub:

1. افتح الملف.
2. اضغط Ctrl+A.
3. اضغط Delete.
4. الصق الملف الجديد كاملًا.
5. Commit changes.

### تحذير مهم جدًا

عند تغيير اسم الملف في GitHub: يجب مسح خانة اسم الملف بالكامل أولًا، حتى لا يحدث خطأ مثل `src/src/...`.

## المرجع المحلي

آخر نسخة معروفة من App.jsx موجودة في:
`/home/claude/contracting-app/src/App.jsx`

هذه النسخة تشمل: الخزينة، السلف والتمويلات (finance_persons/finance_transactions)، **وتبويب التكاليف الجديد بشكل Pivot Table مُجمّع (بند العمل ← تفاصيل بند التكلفة)**.

## طريقة المتابعة

لا تعيد تحليل المشروع بالكامل من البداية. ابدأ مباشرة من الـ Backlog أعلاه أو من طلب المستخدم الحالي.

قبل إجراء تعديل كبير:

* راجع فقط الملفات المطلوبة.
* لا تعدّل قاعدة البيانات إلا عند الحاجة.
* لا تضف مكتبات جديدة إلا إذا كانت ضرورية.
* حافظ على التصميم الحالي والـ RTL.
* بعد أي تعديل على App.jsx، حدّث PROJECT_STATUS.md ليعكس الحالة الجديدة.

---

## سجل التصحيحات

### 2026-08-27 (تصحيح أول)
تمت مراجعة `App.jsx` الفعلي مقابل هذا الملف، وتصحيح تعارضات: (1) وحدة السلف والتمويلات تستخدم `finance_persons`/`finance_transactions` كقسم عام مستقل عن المشروعات — وليس `funders`/`financings`/`financing_repayments` مرتبطة بكل مشروع كما كان موصوفًا سابقًا. (2) علاقة `work_items → costs` هي حذف نهائي صريح من الكود، وليست SET NULL. أي إشارة لاحقة إلى البنية القديمة (funders/financings/financing_repayments) لم تعد صالحة.

### 2026-08-27 (تحديث ثانٍ)
تم تعديل تبويب "التكاليف" (`CostsTab`) بالكامل من جدول مسطّح إلى عرض Pivot Table مُجمّع على مستويين: بند العمل ← تفاصيل بند التكلفة (الوصف)، مع دعم الطي/الفتح على كل مستوى وعرض الحركات الفردية للتعديل/الحذف. باقي التبويبات لم تتأثر. راجع قسم "Known Issues" للقيود الجديدة الناتجة عن التجميع بالنص الحرفي للوصف.
