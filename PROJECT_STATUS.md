# Omar ERP — Current Project Context

آخر تحديث: 2026-08-29 (تحديث سادس — صافي الربح والمصاريف المتوقعة المستقبلية، شغّال فعليًا على الإنتاج)

## المشروع

Omar ERP هو نظام إدارة لشركة مقاولات، باللغة العربية RTL، يركز على إدارة المشروعات وتكاليفها وإيراداتها وتقاريرها.

## التقنية

* React 18 + Vite
* Tailwind CSS
* Recharts
* lucide-react
* Supabase (Postgres + supabase-js)
* Vercel + GitHub
* المستخدم يرفع الملفات يدويًا من GitHub Web، ولا يستخدم Git CLI.

## الملفات الأساسية

```text
contracting-app/
├── index.html
├── vite.config.js
├── package.json
└── src/
    ├── main.jsx
    ├── App.jsx
    └── supabaseClient.js
```

`App.jsx` يحتوي معظم الواجهة والمكونات.

## قاعدة البيانات

Supabase project ref:
`pywkkzedqqgmaghdqxba`

الجداول:

* projects (يشمل عمود `treasury_opening_balance` لرصيد بداية الخزينة اليدوي)
* work_items
* costs (يشمل أعمدة `cost_level_1` و`cost_level_2` لتصنيف فرعي اختياري للتكلفة، **وعمود `custody_id` (جديد) لربط أي تكلفة ببند تصفية عهدة**)
* extracts
* collections (**مُحدَّث**: عمود `project_id` جديد — كل تحصيل مربوط بالمشروع مباشرة، مش بس عن طريق المستخلص؛ عمود `extract_id` بقى اختياريًا (nullable) عشان يسمح بتحصيلات مش مرتبطة بأي مستخلص؛ عمود `note` جديد اختياري)
* app_users
* treasury_entries (حركات خزينة كل مشروع: إيداع/صرف)
* finance_persons (أشخاص/جهات السلف والتمويلات — **مستقلة عن المشروعات**)
* finance_transactions (حركات كل شخص: تمويل / سلفة / سداد، في جدول مسطّح واحد)
* **custodies (جديد)** — العُهد، **مربوطة بمشروع محدد** (بعكس finance_persons): `id`, `project_id`, `person_name`, `amount_given`, `date_given`, `status` ('مفتوحة' | 'مصفاة'), `notes`, `source_cost_id` (يُستخدم فقط للعُهد القديمة المُرحّلة تلقائيًا من `costs`، انظر قسم "تصفية العهد" بالأسفل)
* **custody_categories (جديد)** — تصنيفات عامة قابلة للإضافة/الحذف من الواجهة (`id`, `name`)، تُستخدم لتصنيف بنود التصفية غير المرتبطة ببند عمل (مثال: إكراميات، مصروفات إدارية، مصروفات موقع)
* **expected_costs (جديد)** — مصاريف متوقعة مستقبلية (لسه هتتصرف)، مربوطة بمشروع دائمًا، وممكن (اختياري) تربطها ببند عمل معيّن: `id`, `project_id`, `work_item_id` (اختياري), `description`, `amount`, `expected_date` (اختياري), `notes`. **لا تُحتسب كتكلفة فعلية** في `costs` — بتُستخدم فقط لحساب "صافي الربح المتوقع" (توقّعي/Forecast)، ومفيش أي علاقة تلقائية بينها وبين تحويلها لتكلفة فعلية لاحقًا (لازم تُسجَّل التكلفة الفعلية بشكل منفصل لما تتصرف فعلًا، والمصروف المتوقع يتمسح/يتعدّل يدويًا وقتها).

العلاقات الأساسية:

* project → work_items: Cascade
* project → costs: Cascade
* project → extracts: Cascade
* extracts → collections: Cascade (لسه سارية — حذف مستخلص يحذف معاه أي تحصيلات مرتبطة بيه بنفس القديم)
* **project → collections (جديد): Cascade مباشر** — عن طريق `project_id`، مستقل عن `extract_id`. بفضل ده أي تحصيل (سواء مرتبط بمستخلص أو "تحصيل عام" بدون مستخلص) بيتمسح تلقائيًا عند حذف المشروع.
* work_items → costs: **يتم حذف التكاليف المرتبطة صراحةً من الكود قبل حذف بند العمل** (حذف نهائي وليس SET NULL — الكود ينفّذ `delete from costs where work_item_id = id` ثم يحذف البند)
* project → treasury_entries: Cascade
* finance_persons → finance_transactions: **مستقل تمامًا عن جدول projects** — لا توجد علاقة بمشروع. عند حذف شخص/جهة، يتم حذف حركاته من الكود صراحةً (`delete from finance_transactions where person_id = id`) ثم حذف الشخص.
* **project → custodies (جديد): Cascade** — العهدة مرتبطة بمشروع محدد دائمًا.
* **custodies → costs (جديد): SET NULL عبر عمود `custody_id`** — عند حذف عهدة، يتم فك ربط أي تكلفة مرتبطة بها صراحةً من الكود (`update costs set custody_id = null where custody_id = id`) قبل حذف العهدة، مع الإبقاء على التكاليف نفسها كسجلات فعلية.
* **project → expected_costs (جديد): Cascade** — المصروف المتوقع مربوط بمشروع دائمًا.
* **work_items → expected_costs (جديد): SET NULL عبر عمود `work_item_id`** — عند حذف بند عمل، يتم فك ربط أي مصروف متوقع مرتبط بيه صراحةً من الكود (`update expected_costs set work_item_id = null where work_item_id = id`) قبل حذف بند العمل، مع الإبقاء على المصروف المتوقع نفسه (بيبقى "غير مرتبط ببند").

عند حذف Work Item يتم حذف التكاليف المرتبطة به صراحةً من الكود قبل حذف البند، وفك ربط أي مصاريف متوقعة مرتبطة بيه (SET NULL بدل الحذف).
عند حذف مشروع، يتم حذف بنود الأعمال والتكاليف والمستخلصات والتحصيلات وحركات الخزينة والعُهد **والمصاريف المتوقعة (جديد)** الخاصة به من الـ state صراحةً بعد نجاح حذف المشروع من قاعدة البيانات.

كل الجداول عليها RLS مفعّل مع policy عامة `using (true) with check (true)`، بنفس نمط باقي الجداول العادية (فيما عدا `app_users`، انظر قسم Login).

## Login

يوجد جدول `app_users` يحتوي username/password.
RLS مفعّل بدون policies.
التحقق يتم فقط عن طريق:
`verify_login(username, password)`
وهي Security Definer ومسموحة للـ anon.

لا يتم حفظ تسجيل الدخول في localStorage أو sessionStorage. المستخدم يجب أن يسجل الدخول مرة أخرى بعد إغلاق المتصفح.

**مهم:** لا تعرض كلمة المرور في الشات.

## الحالة الحالية

المشروع منشور ويعمل على Vercel Production.

* **فصل التحصيلات عن المستخلصات: مُفعَّل بالكامل على الإنتاج.**
* **تصفية العهد: مُفعَّلة بالكامل على الإنتاج.**
* **صافي الربح والمصاريف المتوقعة المستقبلية: مُفعَّلة بالكامل على الإنتاج.**

كل الميزات الحالية بما فيهم التلاتة دول تعمل على الإنتاج. الخطوة المتبقية الوحيدة يدويًا: مراجعة أسماء الأشخاص في العُهد القديمة اللي اترحّلت تلقائيًا (ظهرت باسم الوصف القديم للتكلفة بدل اسم حقيقي)، وتصحيحها من شاشة "تصفية العهد" لما يكون فيه وقت.

### المميزات الموجودة

* إنشاء / تعديل / حذف المشروعات، وحذف المشروع بالكامل مع البيانات المرتبطة
* إدارة بنود الأعمال: إضافة / تعديل (الاسم فقط من الواجهة حاليًا) / حذف
* إدارة التكاليف (أنواعها: مشتريات، مصنعيات، مصروفات، عهد، مصروفات عمومية بأصناف جاهزة)، مع حقلين اختياريين "المستوى الأول/الثاني" للتصنيف
* تبويب "التكاليف" — عرض Pivot Table على مستويين (بند العمل ← تفاصيل بند التكلفة/الوصف)، مع فتح/طي وعرض الحركات الفردية للتعديل/الحذف
* المستخلصات: إضافة / تعديل / حذف، وطباعة المستخلص PDF عبر `window.print()`
* **التحصيلات (مُحدَّث ومُفعَّل على الإنتاج):**
  * تحصيلات مرتبطة بمستخلص معيّن (زي الأول بالظبط، من غير تغيير في السلوك).
  * **تحصيلات عامة (جديد)** — تسجيل مبلغ استلمته من العميل (دفعة مقدمة، تحت الحساب، إلخ) **بدون الحاجة لعمل مستخلص أصلاً**، مربوطة بالمشروع مباشرة، مع تاريخ وطريقة تحصيل وملاحظة اختيارية. تظهر في قسم مستقل أعلى تبويب "التحصيلات والمستخلصات".
  * إجمالي "المُحصَّل" في لوحة التحكم بقى بيجمع كل التحصيلات (المرتبطة بمستخلص + العامة) مع بعض تلقائيًا.
* خزينة المشروع: رصيد افتتاحي يدوي، حركات إيداع/صرف، تجميع يومي تلقائي (رصيد أول/آخر اليوم)، تعديل/حذف
* حسابات السلف والتمويلات (قسم عام مستقل، غير مرتبط بأي مشروع): أشخاص/جهات، حركات (تمويل/سلفة/سداد)، كشف حساب تراكمي لكل شخص
* **تصفية العهد (جديد) — تبويب مستقل داخل كل مشروع:**
  * تسجيل عهدة جديدة: اسم الشخص، المبلغ المستلم، تاريخ الاستلام، ملاحظات — دائمًا مربوطة بالمشروع النشط.
  * شاشة تصفية تفصيلية لكل عهدة: إضافة بنود تصفية فعلية، كل بند إما مرتبط ببند عمل موجود في المشروع، أو بتصنيف عام (من جدول `custody_categories` القابل للتعديل من الواجهة عبر زر "إدارة التصنيفات" — إضافة وحذف تصنيفات بدون أي تعديل في الكود).
  * كل بند تصفية هو سجل عادي في جدول `costs` (بنفس آلية العرض والتعديل والحذف الحالية)، مربوط بعمود `custody_id` بالعهدة الأصلية.
  * عرض تلقائي لثلاث قيم: المبلغ المستلم، إجمالي المُصفّى، والمتبقي (عند الشخص أو المستحق له لو صرف أكتر من العهدة).
  * زر "إنهاء وتصفية العهدة نهائيًا": يقفل حالة العهدة (`status = 'مصفاة'`) ويمنع إضافة بنود جديدة بعد كده. لو كانت العهدة مُرحّلة من نظام قديم (`source_cost_id` موجود)، يتم حذف قيد التكلفة الأصلي القديم تلقائيًا عند التصفية لمنع احتساب المبلغ مرتين في الإجماليات.
  * **ترحيل تلقائي للعُهد القديمة:** أي تكلفة كانت مسجّلة سابقًا بنوع "عهد" (كمبلغ إجمالي واحد بدون تفاصيل) تحوّلت عبر SQL migration إلى سجل "عهدة مفتوحة" جاهزة للتفصيل والتصفية من الشاشة الجديدة. اسم الشخص افتراضيًا = الوصف القديم للتكلفة، **ويحتاج مراجعة وتصحيح يدوي**.
* **صافي الربح والمصاريف المتوقعة المستقبلية (جديد):**
  * كارت جديد "صافي الربح (المُحصَّل − الفعلي)" في أعلى صفحة كل مشروع، بيتحسب تلقائي من إجمالي كل التحصيلات ناقص إجمالي التكاليف الفعلية.
  * قسم جديد "مصاريف متوقعة مستقبلية" في تبويب "لوحة التحكم" — إدخال يدوي لأي مصروف لسه هيتصرف على المشروع (وصف، مبلغ، تاريخ متوقع اختياري، بند عمل اختياري، ملاحظات)، **بدون ما يُحتسب كتكلفة فعلية** في `costs`.
  * لو فيه مصاريف متوقعة مسجّلة، بيظهر صف إضافي في أعلى الصفحة فيه: إجمالي المصاريف المتوقعة، و"صافي الربح المتوقع (بعد المصاريف المستقبلية)" = صافي الربح الحالي ناقص إجمالي المتوقع. الصف ده مش بيظهر لو مفيش مصاريف متوقعة مسجّلة.
  * المصروف المتوقع مستقل تمامًا عن التكلفة الفعلية — لما المصروف يتصرف فعلًا، المستخدم لازم يسجّله كتكلفة عادية في تبويب "التكاليف" **ويمسح/يعدّل المصروف المتوقع يدويًا بنفسه** (مفيش تحويل تلقائي أو ربط بين الاتنين حاليًا).
* Dashboard: إحصائيات + رسم بياني ميزانية/فعلي لكل بند + آخر التكاليف
* Budget vs Actual (تبويب "المقايسة")
* RTL / Arabic UI بالكامل، خط Cairo وIBM Plex Mono للأرقام

## الطباعة

طباعة المستخلص موجودة وتعمل في Production عبر نافذة HTML مخصصة و`window.print()`. لا توجد مكتبة PDF إضافية.

## Build / Deploy

Vercel متصل بـ GitHub `main` ويعمل Auto Deploy بعد كل Commit.

**✅ فصل التحصيلات عن المستخلصات:** تم تنفيذ `collections_migration.sql` على قاعدة البيانات الحية، وتم رفع الكود المرتبط بيه على GitHub. الميزة شغّالة على Production.

**✅ تصفية العهد:** تم تنفيذ `custody_migration.sql` على قاعدة البيانات الحية (أنشأ `custodies`، `custody_categories`، عمود `custody_id`، ورحّل العُهد القديمة)، وتم رفع الكود على GitHub. الميزة شغّالة على Production.

**✅ صافي الربح والمصاريف المتوقعة المستقبلية:** تم تنفيذ `expected_costs_migration.sql` على قاعدة البيانات الحية (أنشأ جدول `expected_costs`)، وتم رفع الكود على GitHub. الميزة شغّالة على Production.

**متبقي (خطوة يدوية غير تقنية):** مراجعة أسماء الأشخاص في العُهد المُرحّلة تلقائيًا من النظام القديم (ظهرت مبدئيًا باسم الوصف القديم للتكلفة) وتصحيحها لأسمائها الحقيقية من شاشة "تصفية العهد".

## Known Issues

1. Bundle كبير نسبيًا وفي ازدياد تدريجي.
2. Passwords في `app_users` مخزنة Plain Text — أداة داخلية حاليًا.
3. لا توجد Multi-user permissions حاليًا.
4. حذف بند العمل يحذف التكاليف المرتبطة به حذفًا نهائيًا (وليس SET NULL) — لا يمكن التراجع، ويُطلب تأكيد المستخدم قبل التنفيذ.
5. وحدة "السلف والتمويلات" غير مرتبطة بمشروع محدد إطلاقًا (سلوك مقصود حاليًا) — بعكس "تصفية العهد" الجديدة، اللي مربوطة بمشروع دائمًا (سلوك مقصود كذلك).
6. لا يوجد نموذج ظاهر في الواجهة لتعديل الكمية/سعر الوحدة لبند العمل مباشرة.
7. لا يوجد ربط تلقائي بين تحصيل مستخلص وحركة إيداع في الخزينة.
8. استخدام `alert()` و`window.confirm()` بدلاً من مكوّنات UI متسقة.
9. توليد المعرفات (IDs) على العميل عبر `Math.random().toString(36)`.
10. التجميع الفرعي في تبويب التكاليف يعتمد على **تطابق نص الوصف حرفيًا** (بعد `trim()` فقط) — لو المستخدم كتب نفس البند بصياغات مختلفة هيتحسبوا كبندين فرعيين منفصلين بدل بند واحد مجمّع. لا يوجد حاليًا قائمة موحّدة/autocomplete لأوصاف التكلفة تمنع هذا التشتت.
11. لا يوجد فرز يدوي للبنود الفرعية داخل كل بند عمل — الترتيب حاليًا تلقائي حسب إجمالي القيمة تنازليًا.
12. **(جديد)** العُهد المُرحّلة تلقائيًا من النظام القديم تحمل اسم شخص افتراضي غير دقيق (= الوصف القديم للتكلفة) لحين المراجعة اليدوية من المستخدم.
13. **(جديد)** التصفية حاليًا تُقفل نهائيًا بضغطة واحدة ("إنهاء وتصفية العهدة نهائيًا") بدون خطوة تصفية جزئية/مسودة قابلة للحفظ والعودة إليها لاحقًا قبل القفل — كل بند تصفية يُحفظ فوريًا لحظة إضافته، لكن "القفل" نفسه فعل نهائي واحد.
14. **(جديد)** التحصيلات العامة (بدون مستخلص) مش مربوطة تلقائيًا بأي مستخلص لاحق — لو اتعمل مستخلص بعد كده يغطي نفس المبلغ، المستخدم لازم يراجع يدويًا إن مفيش ازدواجية في العرض بين "التحصيل العام" و"تحصيل المستخلص".
15. **(جديد)** المصاريف المتوقعة مستقلة تمامًا عن التكاليف الفعلية — لما المصروف المتوقع يتصرف فعليًا، مفيش تحويل تلقائي له كتكلفة فعلية ولا حذف تلقائي للمصروف المتوقع نفسه؛ المستخدم لازم يسجّل التكلفة الفعلية في مكانها ويمسح/يعدّل المصروف المتوقع بنفسه، وإلا هيتضاعف الاحتساب في "صافي الربح المتوقع".

## Backlog

المهام القادمة:

1. Multi-users & Permissions
2. Inventory Module
3. Reminders / Scheduling
4. Attachments / Documents
5. نموذج تعديل كمية/سعر بند العمل من الواجهة مباشرة
6. تقييم الحاجة لربط "حسابات السلف والتمويلات" بمشروع محدد (اختياري)
7. إضافة قائمة موحّدة (autocomplete) لأوصاف التكلفة الشائعة لكل بند عمل، لتقليل تشتت التجميع في الـ Pivot Table بسبب اختلاف الصياغة
8. خيار "طي/فتح الكل" لمجموعات التكاليف دفعة واحدة، لو عدد بنود الأعمال أو التفاصيل زاد بشكل كبير
9. **(جديد، مقترح)** إضافة تقرير/كشف مجمّع لكل العُهد المفتوحة عبر كل المشروعات في مكان واحد (حاليًا لازم تدخل كل مشروع لوحده لمتابعة عُهده)
10. **(جديد، مقترح)** دعم "تصفية جزئية" رسمية (حالة وسيطة بين مفتوحة ومصفاة) لو العهدة بتتصفى على دفعات زمنية متباعدة
11. **(جديد، مقترح)** خاصية "ربط تحصيل عام بمستخلص لاحقًا" — تحويل تحصيل عام تم تسجيله قبل عمل المستخلص إلى تحصيل مرتبط بمستخلص بمجرد إصداره، بدل ما يفضلوا سجلين منفصلين
12. **(جديد، مقترح)** زر "تحويل لتكلفة فعلية" على المصروف المتوقع — يحوّله تلقائيًا لسجل في `costs` (بنفس البند/بند العمل) ويمسح المصروف المتوقع، بدل التسجيل اليدوي المزدوج الحالي

## أهم قواعد العمل

* كل البيانات يجب أن تأتي من Supabase.
* ممنوع الرجوع إلى Seed/Mock Local Data.
* `app_users` يجب أن يظل مقفولًا بدون Read Policy.
* الوصول لتسجيل الدخول فقط عبر `verify_login`.
* لا تضف localStorage/sessionStorage لحفظ الجلسة.
* المستخدم يرفع الكود يدويًا عبر GitHub Web.

### عند تعديل App.jsx

لا تعطِ المستخدم Patch جزئي. أعطه **الملف كاملًا**.

تعليمات GitHub:

1. افتح الملف.
2. اضغط Ctrl+A.
3. اضغط Delete.
4. الصق الملف الجديد كاملًا.
5. Commit changes.

### تحذير مهم جدًا

عند تغيير اسم الملف في GitHub: يجب مسح خانة اسم الملف بالكامل أولًا، حتى لا يحدث خطأ مثل `src/src/...`.

## المرجع المحلي

آخر نسخة معروفة من App.jsx موجودة في:
`/home/claude/contracting-app/src/App.jsx`

هذه النسخة تشمل: الخزينة، السلف والتمويلات (finance_persons/finance_transactions)، تبويب التكاليف بشكل Pivot Table مُجمّع، وحدة "تصفية العهد" (custodies/custody_categories)، فصل التحصيلات عن المستخلصات (project_id على collections)، ووحدة "صافي الربح والمصاريف المتوقعة" (expected_costs) — **كل التحديثات دي مُفعّلة بالفعل على الإنتاج**.

## طريقة المتابعة

لا تعيد تحليل المشروع بالكامل من البداية. ابدأ مباشرة من الـ Backlog أعلاه أو من طلب المستخدم الحالي.

قبل إجراء تعديل كبير:

* راجع فقط الملفات المطلوبة.
* لا تعدّل قاعدة البيانات إلا عند الحاجة.
* لا تضف مكتبات جديدة إلا إذا كانت ضرورية.
* حافظ على التصميم الحالي والـ RTL.
* بعد أي تعديل على App.jsx، حدّث PROJECT_STATUS.md ليعكس الحالة الجديدة.

---

## سجل التصحيحات

### 2026-08-27 (تصحيح أول)
تمت مراجعة `App.jsx` الفعلي مقابل هذا الملف، وتصحيح تعارضات: (1) وحدة السلف والتمويلات تستخدم `finance_persons`/`finance_transactions` كقسم عام مستقل عن المشروعات — وليس `funders`/`financings`/`financing_repayments` مرتبطة بكل مشروع كما كان موصوفًا سابقًا. (2) علاقة `work_items → costs` هي حذف نهائي صريح من الكود، وليست SET NULL. أي إشارة لاحقة إلى البنية القديمة (funders/financings/financing_repayments) لم تعد صالحة.

### 2026-08-27 (تحديث ثانٍ)
تم تعديل تبويب "التكاليف" (`CostsTab`) بالكامل من جدول مسطّح إلى عرض Pivot Table مُجمّع على مستويين: بند العمل ← تفاصيل بند التكلفة (الوصف)، مع دعم الطي/الفتح على كل مستوى وعرض الحركات الفردية للتعديل/الحذف. باقي التبويبات لم تتأثر. راجع قسم "Known Issues" للقيود الجديدة الناتجة عن التجميع بالنص الحرفي للوصف.

### 2026-08-28 (تحديث ثالث)
أُضيفت وحدة "تصفية العهد" بالكامل: جدولا `custodies` و`custody_categories` جديدان، عمود `custody_id` في `costs`، ترحيل تلقائي للعُهد القديمة (نوع تكلفة "عهد") إلى عُهد مفتوحة جاهزة للتفصيل، تبويب جديد "تصفية العهد" داخل كل مشروع مع شاشة تفصيلية للتصفية وإدارة تصنيفات عامة قابلة للتعديل من الواجهة.

### 2026-08-29 (تحديث رابع)
تم فصل التحصيلات عن المستخلصات: عمود `project_id` جديد على `collections` (كل تحصيل مربوط بالمشروع مباشرة)، عمود `extract_id` بقى اختياريًا، عمود `note` جديد اختياري. أُضيف قسم "تحصيلات عامة (بدون مستخلص)" في تبويب "التحصيلات والمستخلصات" يسمح بتسجيل مبالغ مُحصَّلة من العميل قبل عمل أي مستخلص. إجمالي "المُحصَّل" في لوحة التحكم بقى يجمع كل التحصيلات (المرتبطة بمستخلص + العامة).

### 2026-08-29 (تحديث خامس) — ✅ الميزتان مؤكَّد تفعيلهما على الإنتاج
تم التأكيد من المستخدم إن كلا الميزتين (تصفية العهد، وفصل التحصيلات عن المستخلصات) تم تنفيذ الـ SQL migrations الخاصة بيهم فعليًا على قاعدة البيانات الحية، وتم رفع الكود على GitHub، والاتنين شغالين على Production بدون مشاكل. لم يتبقَّ إلا خطوة يدوية واحدة غير عاجلة: مراجعة أسماء الأشخاص في العُهد المُرحّلة تلقائيًا من النظام القديم.

### 2026-08-29 (تحديث سادس) — ✅ تم التنفيذ فعليًا على الإنتاج
أُضيف جدول `expected_costs` جديد لتسجيل المصاريف المتوقعة المستقبلية يدويًا (مش لازم تكون مرتبطة ببند عمل موجود)، مستقلة تمامًا عن `costs` الفعلية. أُضيف كارت "صافي الربح (المُحصَّل − الفعلي)" دائم في أعلى صفحة كل مشروع، وصف إضافي شرطي (يظهر بس لو فيه مصاريف متوقعة مسجّلة) فيه "مصاريف متوقعة مستقبلية" و"صافي الربح المتوقع (بعد المصاريف المستقبلية)". أُضيف قسم إدارة كامل (إضافة/تعديل/حذف) للمصاريف المتوقعة في تبويب "لوحة التحكم". عند حذف بند عمل، بيتم فك ربط أي مصروف متوقع مرتبط بيه (SET NULL) بدل حذفه. **تم تشغيل `expected_costs_migration.sql` فعليًا، وتم رفع الكود — الميزة شغّالة على Production.**
